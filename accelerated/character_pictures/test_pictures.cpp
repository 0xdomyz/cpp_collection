// g++ test_pictures.cpp -o test_pictures && ./test_pictures

#include "pictures.h"
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
    // vector<string> vs;

    // // conversion from vector<string> to Picture
    // Picture p = vs;
    // Picture q = frame(vs);

    // // argument symmentry of nonmember functions
    // cat(frame(p), p);

    return 0;
}